{
  "name": "matrix-calculator-app",
  "version": "1.0.0",
  "description": "A single-page web application for matrix calculations and solving equations.",
  "main": "src/js/app.js",
  "scripts": {
    "test": "jest",
    "start": "live-server src --port=8080 --host=localhost",
    "dev": "live-server src --port=8080 --host=localhost --open",
    "server": "node server/index.js"
  },
  "keywords": [
    "matrix",
    "calculator",
    "equation",
    "solver",
    "web",
    "application"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "live-server": "^1.2.1",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "jest": "^27.0.0"
  }
}